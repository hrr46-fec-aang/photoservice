import { shallow, mount, render } from 'enzyme';

describe('My Test Suite', () => {
  it('My Test Case', () => {
    expect(true).toEqual(true);
  });
});

// describe('<App >', () => {
//   it()
// })
